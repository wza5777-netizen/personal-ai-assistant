# Evaluation reports

JSON evaluation reports produced by `uv run python -m evaluation.runner` are
written here (timestamped files plus `latest.json`). These reports are
generated artifacts and are git-ignored.
